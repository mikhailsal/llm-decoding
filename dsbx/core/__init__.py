"""Pure-logic core: config loading, storage preflight, data model, backends.

Intentionally free of UI dependencies (no rich/prompt_toolkit) so the same
engine can power the CLI today and a web UI later.
"""
